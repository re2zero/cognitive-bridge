{
  "name": "@re2zero/cog",
  "version": "0.2.0",
  "description": "轻量级认知增强插件：为 pi 注入身份认知 + 动态 NAP 叙事锚点",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "cog": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "pi",
    "cognitive",
    "narrative",
    "anchor",
    "identity",
    "emotion"
  ],
  "license": "MIT",
  "dependencies": {
    "better-sqlite3": "^13.0.1"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=0.0.1"
  },
  "peerDependenciesMeta": {
    "@opencode-ai/plugin": {
      "optional": true
    }
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.18.6",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^3.2.7"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "dist/",
    "config/",
    "README.md"
  ],
  "allowScripts": {
    "better-sqlite3@13.0.1": true
  }
}
